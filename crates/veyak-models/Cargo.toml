[package]
name = "veyak-models"
version = "0.1.0"
edition = "2024"

[dependencies]
ts-rs = { workspace = true }
serde = { workspace = true, features = ["derive"] }
reqwest = { workspace = true,  features = ["json", "multipart", "query", "form", "native-tls"] }
